/*const mysql = require('mysql');

let INSERT_NAME_QUERY = 'INSERT INTO names(firstName, lastName) VALUES(Chris,Brewster)';
const SELECT_ALL_SERVICEUSER_QUERY = 'SELECT lastName FROM names';
const SELECT_ALL_HOUSES_QUERY = 'SELECT * FROM House';
const SELECT_ALL_WORKERS_QUERY = 'SELECT * FROM Worker';
const SELECT_ALL_ROOMS_QUERY = 'SELECT * FROM Room';

const houseConnect = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'SaviourTrust44',
    port: 3306,
    database: 'House'
  });

connection.connect();



var names = {
    firstName: document.getElementsByName(),
    lastName: 'LeBouf',
    gender: 'Male'
  };
  
var query = connection.query('insert into names set ?', names, function (err, result)
  {
    if(err)
    {
      // if there is an error it will be displayed on the console
      console.error(err);
      return;
    }
    console.error(result);
  });
  
  //connection.query(INSERT_NAME_QUERY);

/*
  // getting all the service users and printing to console log
  connection.connect(function(err) {
    if (err) throw err;
    console.log("Connected!");
    connection.query(SELECT_ALL_SERVICEUSER_QUERY, function (err, result) {
      if (err) throw err;
*/
    

  

  
*/
